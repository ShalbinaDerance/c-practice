#include <stdio.h>
int main()
{
    int N, score, total = 0, highrisk = 0;
    scanf("%d", &N);
    for(int i=0;i<N;i++) {
        scanf("%d",&score);
        total += score;
        if(score  >50){
        highrisk++;
    }
}
    printf("Total Risk: %d\n", total);
    printf("High Risk Sessions: %d", highrisk);
    return 0;
}
