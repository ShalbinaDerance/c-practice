#include <stdio.h>
int main() {
    int num;
    scanf("%d",&num);
    if (num>0) {
        printf("It is positive number:");
    }
    if (num<0) {
        printf("It is negative number:");
    }
    if (num==0) {
    printf("It is zero:");
    }
    return 0;
}