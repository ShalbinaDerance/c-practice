#include <stdio.h>
int main() {
    int N,target;
    scanf("%d",&N);
int price;
int min=0;
int found=0;
for(int i=0;i<N;i++) {
scanf("%d",&price);
if(price>0){
    if(found||price<min){
        min=price;
        found=1;
    }
}
}
if(found)
printf("%d",&min);
else
printf("No positive");
    return 0;
}