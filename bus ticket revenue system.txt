#include <stdio.h>
int main() {
    int N;
    int ticket;
    int total=0;
    scanf("%d",&N);
    for(int i=0;i<N;i++){
        scanf("%d",&ticket);
        total+=ticket;
    }
        printf("Total Collection:%d",total);
    return 0;
}